#pragma once

class ConnectionManager {
public:
	ConnectionManager ( );
	~ConnectionManager ( );

private:

};

ConnectionManager::ConnectionManager ( ) {
}

ConnectionManager::~ConnectionManager ( ) {
}
