#pragma once

#include "Utils.hpp"
#include "BpTree.hpp"

class IndexManager {
public:
	IndexManager ( );
	~IndexManager ( );

	

private:

};

IndexManager::IndexManager ( ) {
}

IndexManager::~IndexManager ( ) {
}

using IndexManagerPtr = shared_ptr<IndexManager>;
