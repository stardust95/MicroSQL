#pragma once

#include "Utils.hpp"

class SystemManager {
public:
	SystemManager ( );
	~SystemManager ( );

private:

};

using SystemManagerPtr = shared_ptr<SystemManager>;

SystemManager::SystemManager ( ) {
}

SystemManager::~SystemManager ( ) {
}
