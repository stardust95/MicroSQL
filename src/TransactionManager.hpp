#pragma once

#include "Transaction.hpp"

class TransactionManager {
public:
	TransactionManager ( );
	~TransactionManager ( );

private:

};

TransactionManager::TransactionManager ( ) {
}

TransactionManager::~TransactionManager ( ) {
}
